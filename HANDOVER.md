# AiryView 開発引き継ぎ

更新日: 2026-09-26
開発場所: C:\dev\projects\utilities\AiryView

現在の導入済みビルドは2.0.18。ページごとの用紙・向きの自動選択を追加し、GitHub公開を準備中。2026-09-26の変更・検証・公開結果は末尾を参照。印刷修正はdocs/PRINTING-2.0.18.md、起動速度はdocs/PERFORMANCE-2.0.17.mdを参照。以下の旧版記録は履歴。


## 最新状態：デスクトップ版1.1（2026-09-14）

最新操作と保存状況は末尾の「軽量保存 @@@」を参照。固定実行先は C:\Program Files\airyPDF\airyPDF.exe。最新配布ZIPとGitHub最新Release ZIPは164327版。

Adobeからの移行向けにパスワード読込、標準AcroFormの記入、欄のないPDFへの日本語追記、付箋、検索、しおり、PFX/P12署名とオフライン検証を実装。Program Filesへ管理者確認付きで登録する構成に変更。詳細と未対応はREADME、実装・検証履歴は末尾の「1.1 PDF互換機能」を参照。
利用者提示のAdobe製2ページ・80文字欄のフォームで日本語保存、権限保持、80欄と5しおりの画面読込を確認。原本・個人情報・証明書はGitと配布一式に含めない。仲間へ渡すセットアップZIPはartifacts/releaseに生成し、まだ外部送付していない。
## 正式デスクトップ版1.0（2026-09-14）

- 利用者が会社で印刷し「寸法は0.1mmくらいずれて見えるが用途では完璧」と報告。用途上合格として記録。厳密な測定条件やAcrobat比較の結果は未取得。
- デスクトップ版への格上げ依頼により1.0.0へ更新。タイトルはairyPDF 1.0、試験向けの明日の案内を普段使いのヘルプに変更。
- Windowsのユーザー別アプリ一覧とPDFの「プログラムから開く」に登録。既定アプリは変更しない。--unregisterは確認後にアプリ登録と自分のリンクのみ解除し、ファイル・設定は残す。
- 画面サイズと最大化状態をLocalAppData/airyPDF/window.jsonに記憶。印刷倍率は新PDF100%を維持。
- 以降の旧記録は実装履歴。会社での印刷未確認という旧記録は今回の利用者報告で更新。両面や小冊子は個別に確認が必要。

## 現在地

ユーザーの「続けてどんどん作って」により実装へ移行。C#・WPF・.NET 10とPDFiumによるWindows x64試作版0.2.1を作成。要件の正本はREQUIREMENTS.md、起動方法と制約はREADME.md。技術の選択や細部は実装時の初期判断であり、ユーザーが個々に明示承認したものとは区別する。

GitHub: https://github.com/MitsunobuSuhara/airyPDF （非公開、origin/main）。ユーザーは他プロジェクトと同じアカウントへの保存を確認済み。初回資料コミット5d9aa54、保存先記録e5283e7。

## 実装したこと

- PDFを複数タブで開く、ドラッグ＆ドロップ、ページ移動、幅合わせ、ホイールスクロール、Ctrl＋ホイールズーム。
- 現在／全ページを90度回転、別名保存、未保存変更の確認。
- 印刷プレビュー、原寸・指定倍率、用紙に合わせる、2／4アップ、小冊子の左右とじ・空白補充。
- 現在ページの矩形範囲印刷、ポスター分割と重なり幅。
- 用紙・向き・ページ範囲・部数・片面／両面。設定変更後はプレビュー更新まで印刷ボタンを無効にする。
- 新しいPDFの印刷倍率は100%。原寸・指定倍率では自動縮小せず欠けを知らせる。
- 印刷配置はmm単位で共通化。実印刷は画面画像を使わずPDFiumからWindowsのプリンター描画領域へ出力。
- 黒単色背景・白い羽根の採用PNGをアプリ画面に使用。原本はassets/icons/airyPDF-black-source.png。

## 確認済み・重要な修正

- Releaseビルド、PDF読込・回転保存・原本保護・ページ範囲・倍率・Nアップ・小冊子・ポスターの自動テスト。
- Microsoft Print to PDFでA4縦50%／100%、A3横200%を出力し、縦横の基準線を画像上で測定。検出した線の外形は50.25／100.25／200.25mm（4px/mmの画像測定。線幅と画素化を含むため紙上の誤差ではない）。
- PageSettings.PrintableAreaが横向き時も縦寸法を返す問題を実測で発見。CreateMeasurementGraphicsに同じPageSettingsを渡してDCから用紙・印刷可能範囲を取得する方式へ修正。
- WPF実ウィンドウの表示画像を保存して目視。印刷ボタンは左下に固定。
- 通常の実行・ファイル編集ツールとcomputer-useはsandbox helper起動エラーが出る。シェルのrequire_escalatedで実行可能。Gitは初期化時の所有者がsandboxユーザーなので、必要に応じgit -c safe.directory=C:/dev/airyPDFを使用（グローバル設定は未変更）。

## 環境・起動

- .NET SDK 10.0.401を .tools/dotnet へ導入。既存PCの.NET 8には依存しない。
- ビルド・テスト・実行用フォルダ作成: scripts/build.ps1 -Test -Publish
- 実行用: artifacts/app/airyPDF.exe（.NET同梱、フォルダ全体を保持）。
- テストPDF・画像・結果: artifacts/。SDK、ドライバー、ビルド生成物はGit対象外。
- PDFiumパッケージ155.0.8044を固定。ラッパーは使わず直接P/Invoke。関連ライセンスはTHIRD-PARTY、実行フォルダにも同梱。更新はライセンスとテストをセットで行う。

## Apeos C3571ドライバー

ユーザーの「取得して」により公式ART EXドライバー7.1.10、Windows 11 64ビット用を取得済み。
- ファイル: artifacts/drivers/ffopkplw250330w646fml.exe
- 29,382,112バイト。FUJIFILM Business Innovation Corp.の電子署名Validを確認。
- SHA256: 12F6C2105308E7B21BF191BB6FBA79905E2880E65A11950AD958E89137B9B9C4
- 公式ページ: https://www.fujifilm.com/fb/ja/support/multifunction-printers/color/download-00851
- 公式ドライバーを管理者権限で導入済み。FF Apeos C3571、キュー名「Apeos C3571 (ART EX)」。ユーザーのスクリーンショットから得たIPへTCP/IPポートを登録。導入ログはartifacts/drivers/install-result.txt。社内IPとドライバー実体はGitへ含めない。実機への通信・紙出力は未確認。

## 0.2で追加・導入したこと

- .NET同梱アプリをユーザーのLocalAppData/Programs/airyPDF/0.2.0-ハッシュへコピーし、デスクトップとスタートメニューへ登録。--installで再登録できる。元の版は削除しない。
- 採用PNGから7サイズのICOを作り、アプリとショートカットに設定。
- 「寸法確認用PDF」「使い方・登録」ボタンを追加。確認PDFはassets/checks/print-check.pdf、配布先Samples/print-check.pdf。A4縦・A4横・A3横・A4縦の4ページ、縦横100mm目盛付き。Popplerで全ページ描画して目視済み。
- プリンター名を一覧選択できるようにし、「プリンターの設定」で選んだドライバーの設定画面を開く。ネイティブ設定画面内の操作は未検証。
- Releaseビルド警告・エラー0。33件のPDF・印刷検証と、17件のUI検証に合格。Apeosを含む全3プリンター切替はプレビューのみ検証。物理プリンターには印刷ジョブを送っていない。
- 登録先から確認PDFを開いて起動。デスクトップ・スタートメニューのリンク先を確認済み。
- scripts/install-apeos.ps1は管理者実行用。初回のUACキャンセル後、ユーザーの「お願いします」で再実行し導入成功。以後、同じ導入を繰り返す必要はない。
## 連続スクロール追加（2026-09-13）

- ユーザーの依頼で全ページを縦に連続配置。スクロール位置に応じて現在ページ番号を更新し、ページ移動ボタンと番号入力も維持。
- 画面付近のみ描画し、画面外の画像は解放。見えているページ群の描画予算は合計約12MP。ページ枠は全ページ作るため極端なページ数の性能は今後確認。
- ページ境界の同時描画、最終ページ移動、ページ番号、画像解放、拡大後の位置、先頭への復帰をUIテストで追加し合格。境界の画面画像を目視。
- デスクトップ登録先も更新。起動中の古い版は未保存変更を保護するため強制終了しない。

## 0.2.1 起動先修正（2026-09-13）

- ユーザーの4ページPDFが1ページ目しか見えない報告を調査。PDF自体は4ページ。原因はタスクバーピンが連続表示追加前の0.2.0-1b04d7923fd7を起動していたこと。デスクトップだけ更新済みだった。
- インストーラーで既存のairyPDFタスクバーリンクも更新。CodexパッケージのLocalCache経由の旧インストール先も自分のリンクと判定。タイトルを0.2.1・連続スクロール対応とし、古い版と識別可能にした。
- 実際の業務PDFを読取専用で検証。ホイール入力イベントで1ページ目を越えるテストを追加し、4/4の表示画像を確認。業務PDFと画像・パスはGit対象外。AIRYPDF_UI_PDF環境変数で実ファイルの閲覧検証が可能（複数ページ限定）。印刷テストは合成PDFのまま。
- 更新後のタスクバーリンク先を確認し、対象PDFを最新版で起動済み。旧版には通常終了のみ要求。

## タスクバーの白い紙アイコンを修正（2026-09-13）

- ユーザー提供画像で白い紙アイコンを確認。リンクのTargetPathはパッケージのLocalCache実パスだがIconLocationは仮想LocalAppDataのままだった。
- GetFinalPathNameByHandleでコピー先exeの実パスを取得し、TargetPath・WorkingDirectory・IconLocationを統一。保存後にSHChangeNotifyでリンクの更新を通知。
- 実パスが同じ内容を指すことと接頭辞除去を検証。Releaseと全テスト合格。再登録後の参照先一致とexeから羽根アイコンを抽出できることを目視確認。タスクバー実表示はユーザーへ確認中。
- 公式API資料: https://learn.microsoft.com/en-us/windows/win32/api/fileapi/nf-fileapi-getfinalpathnamebyhandlew と https://learn.microsoft.com/en-us/windows/win32/api/shlobj_core/nf-shlobj_core-shchangenotify

## 次に着手すること

1. ユーザーはこのPCを会社へ持参する。登録済みのApeosを選び、寸法確認用PDFをA4縦・ページ1・100%で1枚出力。Acrobat Readerと同条件で紙上の縦横を実測する。次にページ3でA3横を確認。READMEに手順あり。
2. 特殊なページ領域、既存回転、フォント、画像、透明効果、フォーム、電子署名付きPDFの扱いを検証。現在はパスワード付き／512MB超を非対応として案内。
3. ポスターの切り取り線と紙上番号、アイコン外周透過整備、表示キャッシュと代表図面の性能改善を進める。
4. 設定記憶・検索・しおりは未確定。試作では未実装。原寸印刷などの必須機能を完成要件から落とさない。

紙上の寸法精度、Acrobat Readerとの同等性、実機両面・小冊子の折り順は未検証。試作の完了を製品の精度保証と扱わない。原寸自動縮小なし・新PDF100%・採用アイコン以外の細部は、ユーザーの個別承認ではなく試作の初期仕様。

## 共通指示

C:\dev\AGENTS.md適用。日本語、呼称NOBUさん。区切りでコミットを提案し、コミットした変更は承認済みoriginへpush。AI署名footerなし。1リポ1エージェント。

## 新アイコンの採用決定（2026-09-14）

ユーザーからメインへの引き継ぎ：新アイコンは比較画像の中央にあるB案を採用。
- 黒の単色背景。
- 3つの白い面で表した羽根。
- グラデーションなし。

比較画像の原本：C:/Users/mitsu/.codex/generated_images/01a09d29-d8c6-7d12-9c57-f170376b3dec/exec-91e8d48b-0aaf-4b5e-97cd-9351be59cc69.png
採用仕様は確定。現行PNG・ICOへの置き換えはこの引き継ぎでは実施していない。画像のツール表示は処理できず、中央B案の形状の目視確認は未完了。単体B案を準備し、アプリ・ショートカット・タスクバーへ反映すること。

## B案アイコン反映済み（2026-09-14）

ユーザーの「実行して」により採用B案をアプリへ反映。
- 比較画像を閲覧用に読み出し、中央B案の3面の羽根を目視確認。画像生成ツールでB案を単体化し、assets/icons/airyPDF-b.pngへ保存。黒単色背景、白い3面の羽根、グラデーションを付けない指定。
- アプリ画面・ウィンドウのPNG参照とmake-icon.ps1を新PNGへ切替。7サイズICOを再生成。従来PNGは履歴・比較用に残す。
- バージョン1.0.1へ更新。既存のデスクトップ・スタートメニュー・タスクバーリンクを更新し、実パスによるアイコン登録を維持。
- Releaseビルドと既存PDF・印刷・連続スクロールのテストに合格。新しいアイコンの反映は寸法配置処理を変更しない。

## 1.1 PDF互換機能とProgram Files構成（2026-09-14）

ユーザーはAdobeからの移行のため、パスワード、入力欄、電子署名、注釈・検索・しおりの追加を依頼。既存の入力欄と欄のないPDFへの記入の両方を希望。証明書はまだ未取得。最終的に仲間へ渡せる一式の準備も明示依頼（以前の外部配布停止方針に対する、このプロジェクトでの新しい指示）。外部送付はまだ行わない。

- バージョン1.1.0。インストール先はユーザー指定の C:\Program Files\airyPDF、配下に版とハッシュのフォルダを作る。管理者確認付き。設定はユーザー別LocalAppData。旧版とユーザーのPDFは削除しない。
- パスワード入力で暗号化PDFを開く。文字・チェック・単一選択の標準AcroFormを編集し、値と外観を保存。日本語の外観はReportLabで埋め込み、フォームをフラット化せず保持。
- 一般編集禁止でもフォーム入力が許可されたPDFはCanFillで対応。署名済み文書の編集・回転・保存は禁止して原本を保護。
- 欄のないPDFへの日本語追記、付箋作成と既存注釈一覧、しおり追加と既存しおり移動、全文字検索のページ一覧（Ctrl+Fの操作画面）。しおりの未定義移動先は無視して読み込みを続ける。
- PDFiumのフォーム専用FFLDrawを追加。フォーム情報はversion 1、JavaScript・自動外部アクションは実行しない。画面に入力欄を描画し、印刷は本文の直接描画に入力欄だけ透過画像を重ねる。
- PFX/P12証明書でPAdES署名を別名保存。pyHankoで署名値・改ざん・Windows ROOTへの信頼・変更・対象範囲を別にオフライン検証。オンライン失効確認は未実施と明示。自己署名を信頼済みと誤表示しない。
- 入力・署名処理はPyInstallerの補助exeとしてHelperへ同梱し、利用者のPythonインストールは不要。pypdf、ReportLab、pyHanko等の固定依存はsrc/pdf-helper/requirements.lock.txt。ライセンスはTHIRD-PARTY/pdf-helper。
- scripts/build-pdf-helper.ps1で補助エンジン＋機能テスト、scripts/build.ps1 -Test -Publishでアプリ、scripts/package.ps1でSetup.cmdと実行一式のZIPを作る。パッケージにはドライバー・社内IP・個人PDF・証明書・テスト結果を含めない。コード署名証明書は未導入。
- 合成PDFで日本語フォーム値・見た目、チェック、追記・注釈・しおり、暗号化保存、証明書署名、改ざん検出、署名済み編集禁止を検証。PDFium読込・検索・画面とMicrosoft Print to PDFへのフォーム印刷、従来の50/100/200%寸法テストに合格。
- 利用者提示のAdobe Acrobat Standard DC製2ページ・80文字欄の暗号化AcroFormを、このPCだけで検証。一般編集禁止・入力許可のまま日本語を検証コピーへ保存し、2ページ・80欄保持と画面での80欄・5しおり読込を確認。PDFiumとPopplerの表示を目視。原本を変更せず、本文・個人情報・検証コピーはGit/パッケージに含めない。
- 個人PDFのローカル検証用コピーはartifacts/sample-compat.pdf。AIRYPDF_TOOL_PDFで入力画面の実ファイルテスト、AIRYPDF_COMPAT_PDFでネイティブ描画と権限確認を実行できる。環境変数にはローカルパスのみ指定。

未対応をREADMEに明記：XFA、JavaScript自動計算、ラジオ・押しボタン変更、複数選択、リッチテキスト、OCR、検索ハイライト、注釈削除・他種作成、しおり編集削除、複数署名追加、ICカード、タイムスタンプ・LTV情報付加、オンラインOCSP/CRL。標準形式での互換性を確認した段階であり、Adobe全機能同等と扱わない。

1.1互換性の補足：保存時は変更した欄だけを書き換え、未変更欄の外観を保持。文字数制限とcomb（1文字ごとに枠がある入力欄）を元のフィールド構造から読み、文字数超過を拒否するテストを追加。Program Files導入先とデスクトップ・スタートメニューの参照一致を確認。タスクバーピンは現在存在しないため、存在しないピンを新規固定せず維持。

## 1.1 固定インストール先（2026-09-14）
配布先で見つけやすくするため、実行先とWindows登録を C:\Program Files\airyPDF\airyPDF.exe に固定。変更対象の既存ファイルがロック中ならコピー前に停止して閉じる案内を出す。利用者の整理依頼に基づき、最新版のハッシュ・登録・未使用・通常ディレクトリを確認して旧ハッシュ版を削除。Release、PDF/印刷自己検証、UI検証、実インストールのハッシュ一致を確認済み。最新版セットアップ: artifacts/release/airyPDF-1.1-20260914-093526.zip。

## 原本に合わせる印刷プレビューとライブ更新（2026-09-14）
利用者は向きだけでなく原本の用紙サイズに合わせる意味と訂正。表示中ページの短辺・長辺を用紙一覧と照合して初期用紙を選び、横縦も原本から選ぶ。非対応サイズは利用可能な用紙へフォールバック。設定イベントは250msのタイマーで自動更新し、変更直後は古い設定で印刷させない。手動更新ボタンは除去。A4横、A3横、倍率ライブ更新と連続入力をUIテストに追加。
検証結果: Releaseと印刷自己検証、UI検証が成功。A4横・A3横、50%自動反映、75%への連続入力を確認。このPCの固定パスのDLLが生成物と一致。最新セットアップ: artifacts/release/airyPDF-1.1-20260914-094406.zip。GitHub送信は自動承認審査で今回分の明示承認不足として拒否。ローカル保存済み、push待ち。

## 閲覧ツールバー整理（2026-09-14）
利用者の画面指摘に対応。表示倍率を入力欄にしてEnter／フォーカス移動で確定、100％復帰ボタンを追加。倍率は10～800％、不正値は元に戻す。画面幅に合わせるへ改称。回転は矢印アイコン、全ページ適用はその他メニューへ。タブを閉じる（Ctrl+W）、寸法確認PDF、使い方もその他へ。範囲選択の閲覧実装を削除、印刷側の範囲チェックも非表示。上段は開く・印刷・保存・編集／検索・その他に整理。
検証: Release、PDF/印刷自己検証、UI検証が成功。倍率100・125%入力、NaN拒否、100%復帰、範囲選択削除を確認。実ウィンドウ画像を目視し、このPCのインストールDLL一致を確認。GitHub送信は前回分から承認待ち。

## 倍率操作の調整（2026-09-14）
利用者の追加指示で100％ボタンを削除、画面幅に合わせるを操作バー右端へ移動。Ctrl＋ホイールは100％をまたぐ1回を原寸で止め、次の操作では先へ進む。拡大縮小の両方向と100％から先へ進めることをUI検証に追加。
検証: Release、PDF/印刷、UI検証が成功。100％停止の上下両方向、停止後の拡大縮小継続、右端配置、100％ボタン削除を確認。浮動小数の原寸付近は正確に1へ補正。このPCのDLLと生成物一致。

## 印刷プレビューのホイール切替（2026-09-14）
利用者依頼で印刷プレビューのスクロール領域にホイール切替を追加。下端で次の面、上端で前の面。途中は通常スクロール。次面は上端、前面は下端に合わせる。設定更新中・面描画中は切替を抑制。先頭と最後では範囲外へ進めない。Nアップ・小冊子・ポスターもsheetsの面順で対応。
検証: Release、PDF/印刷、UI検証に成功。ホイールから次面・前面へ切替、先頭の境界を実画面イベントで確認。このPCのDLLと生成物一致。外部送信は前回から承認待ちのためローカル保存のみ。

## 印刷数値欄の操作（2026-09-14）
倍率・部数・貼り合わせ幅はクリック時とフォーカス取得で全選択。倍率・部数には上下矢印ボタンを追加しキーボード上下にも対応。1％／1部刻み、倍率1～1000・部数1～999。貼り合わせ幅は利用者追加指示でポスター選択時のみ入力欄と説明を表示。通常モードでは非表示で値を使わない。
検証: Release、PDF/印刷、UI検証に成功。3入力欄の全選択、矢印による1％・1部増加、部数下限、通常時非表示とポスター時表示を確認。このPCのDLLと生成物一致。GitHub送信は既存の承認待ちのためローカル保存のみ。

## ページ範囲の入力案内（2026-09-14）
利用者依頼でページ範囲入力欄の直下に1-3、1,3、1-3,5、空欄の意味を常時表示。ツールチップだけでは分からなかった点を修正。Releaseビルド・発行に成功。
このPCのインストールDLLと生成物の一致を確認。変更はローカル保存、GitHub送信は承認待ち。

## 印刷ボタンの常時表示（2026-09-14）
利用者スクリーンショットで印刷ウィンドウの下部が画面外になりボタンが隠れる問題を確認。印刷ボタンを設定側の左上へ固定し、SourceInitializedで所有者のモニターの作業領域とDPIから大きさ・位置を調整。低い画面では最小高さも下げる。1024×600相当でウィンドウ全体と印刷ボタンの位置をUI検証。
検証: Release、PDF/印刷、UI検証に成功。小画面で印刷ボタンが左上に見えることを画像でも確認。ただし今回のUACは利用者によりキャンセルされたため、このPCの実インストールは旧版のまま。修正版セットアップから再実行が必要。GitHub送信は承認待ち、ローカル保存のみ。
追記: 利用者の「実行して」で修正版Setup.cmdを再実行し、C:\Program Files\airyPDF\airyPDF.dllが修正版セットアップのDLLと一致することを確認。このPCへの更新は完了。

## 印刷ボタンの下部配置へ訂正（2026-09-14）
利用者はボタンは下部がよく、ウィンドウ全体が表示される位置で開けばよいと訂正。ボタンを左下の固定位置へ戻し、モニター作業領域とDPIに応じる大きさ・位置調整を維持。小画面で下部ボタンが見えることにUI検証を変更。
検証: Release、PDF/印刷、UI検証に成功。小画面でウィンドウ全体と左下ボタンの表示を確認。このPCのDLLと生成物一致を確認して更新完了。GitHub送信は既存の承認待ち、ローカル保存。

## Ctrl＋Enterで印刷開始（2026-09-14）
利用者依頼で印刷プレビューのCtrl＋Enterを印刷コマンドに割り当て。ボタンと同じ開始処理を使い、ボタン無効／空レイアウトでは開始不可。キー長押しの繰り返しEnterを抑制。ツールチップに案内。UI検証ではキー割当、印刷可能状態、不正倍率時の無効化を確認し、物理プリンターへ試験ジョブは送らない。
検証: Release、PDF/印刷、UI検証に成功。このPCのDLLと生成物一致を確認して更新完了。変更はローカル保存、GitHub送信は既存の承認待ち。

## GitHub Release登録（2026-09-14）
利用者が最新セットアップZIPをGitHubへ入れることを明示依頼。未送信のソースを3d09ef8までorigin/mainへpushし、同一ソースにv1.1.0 Releaseを登録。ZIP: airyPDF-1.1-20260914-103303.zip、112842301バイト、SHA256 473ACC79C540091716A67DEF33BFBC03C0D5CFE1E0D8A45E1BB9664BF4469CB7。GitHub側digestと一致しuploadedを確認。URL: https://github.com/MitsunobuSuhara/airyPDF/releases/tag/v1.1.0 。非公開を維持し、受取人の閲覧権限が必要。旧記録のpush待ちはこの依頼で解消。

## 画面幅調整後の100％停止修正（2026-09-14）
利用者から画面幅調整後のCtrlホイールと矢印ボタンで100％停止しない指摘。＋／−に停止処理がなかったため段階倍率操作を共通化。ホイール連続イベントですぐ通過する点は100％到達時450msの停止で対策。フォーカス中の倍率欄も操作後の倍率で同期する。画面幅調整後のホイール連続入力・＋・−をUI検証に追加。
検証: Release、PDF/印刷、UI検証に成功。画面幅調整後の連続ホイール・＋・−の100％停止を確認。このPCのDLLと生成物一致を確認して更新完了。最初の一時ロック時は安全停止し再実行で完了。今回修正はローカル保存、既存GitHub Releaseは更新前の1.1セットアップのまま。

## 印刷送信後の自動閉鎖（2026-09-14）
利用者依頼で全ページの送信が成功しEndPrintで取消なしのとき印刷プレビューを閉じる。取消・例外では画面を残す。EndPrintを解除してからCloseし、途中でprinterをDisposeしない。ボタンに『この設定で印刷（Ctrl＋Enter）』を常時表示する追加指示も反映。Microsoft Print to PDFの実送信と取消コントローラーで検証。
検証: Release、PDF/印刷、UI検証に成功。仮想プリンターへの実送信で閉じること、取消では保持を確認。ボタン案内追加後もビルド・発行し、このPCのDLLと一致を確認して導入完了。ローカル保存、GitHub Releaseは旧セットアップのまま。

## 軽量保存 @@@（2026-09-14）
### 完了したこと
- インストール先を C:\Program Files\airyPDF\airyPDF.exe に統一。このPCへ最新の修正版を導入済み。
- 原本サイズ・向きに合わせる印刷プレビュー、設定のライブ更新、ホイールによる前後面切替。
- 閲覧ツールバー整理、表示倍率手入力、100％ボタン削除、回転アイコン、範囲選択削除、画面幅に合わせるを右端へ。
- Ctrl＋ホイールと＋／−は原寸をまたぐと450ms停止。画面幅調整後の操作も検証済み。
- 印刷数値欄クリックで全選択、倍率・部数の上下矢印、ページ範囲の入力例、ポスター時だけ貼り合わせ幅を表示。
- 印刷ボタンは左下固定。モニター作業領域とDPIに合わせてウィンドウを画面内へ配置。
- Ctrl＋Enter印刷と常時案内、送信成功後の自動閉鎖、取消・送信失敗では画面保持。Microsoft Print to PDF実送信・取消で検証。
- 最新ローカルセットアップ: artifacts/release/airyPDF-1.1-20260914-104624.zip（SHA256 BD2546354FDE3120EBF3528E425418DC16FB55C4B4F6B14B426200918AF5E9D7）。
- GitHub Release v1.1.0のZIPは103303版のまま。最新の100％停止・印刷後閉鎖はまだRelease ZIPへ反映していない。
- 整理: artifacts/cleanup-old-installs.ps1、artifacts/cleanup-fixed-install.ps1を削除。旧版フォルダ整理が完了し再使用不要な生成スクリプト。Git管理外でそのままの復元不可、必要なら履歴にある安全確認方法で再生成可能。結果ログ・検証資料・元PDF・配布物は保持。
### 次に着手すべき
- 利用者の実印刷で自動閉鎖とCtrl＋Enterを確認。
- 最新修正版を仲間へ渡す場合、上記104624セットアップを使用。GitHub Releaseを更新するなら新しい識別子で公開範囲を維持して登録する。
### 重要な設計原則・未確定論点
- 原本PDFを上書きせず、印刷倍率100％を初期値にする。表示倍率と印刷倍率は別。
- プレビュー更新中・不正入力では印刷不可。送信成功はスプーラーへの送信完了で、紙の排出完了ではない。
- 署名はPFX/P12、オフライン検証。XFA、オンライン失効確認、ICカード、既存署名への追加署名は未対応。
- 非公開GitHubを維持。利用者の仲間への配布希望はairyPDFについての明示指示。個人PDFや証明書をGit・配布物へ含めない。

## 100％表示の描画品質（2026-09-14）
利用者画像で細字が少しぼやける指摘。閲覧画像を画面DPIの最大2倍で描画し、高品質縮小・画素位置の丸めを適用。画面付近のみ12MPの画像予算を維持し表示倍率の寸法と印刷描画は変更しない。DPI変化時も再描画。100％画像の高解像度、寸法保持、画像予算をUI検証に追加。元PDFが低解像度画像の場合の細部復元はできないため、利用者の同じPDFで見え方の確認を要する。
検証: Release、PDF/印刷、UI検証に成功。100％画像の高解像度・寸法保持・画像予算を確認。このPCのDLLと生成物の一致を確認して導入完了。同じ利用者PDFで改善の体感確認待ち。変更はローカル保存、GitHubのRelease ZIPは103303版。

## ブラウザ比較後の文字描画（2026-09-14）
利用者の比較画像では2倍描画の縮小後も文字が薄く見える。閲覧専用RenderにFPDF_LCD_TEXTを追加し、画面画素数へ直接描画して中間縮小を避ける。実配置サイズとDPIから整数画素数を決めNearestNeighborを指定。印刷側と他のRender呼出しは既定のまま。前節の最大2倍描画は撤回。
同じ契約書PDFでUI検証と原寸画像比較を実施し、ブラウザに近い文字の濃さ・輪郭を目視確認。元PDF未変更。FPDF_LCD_TEXTの一次資料: https://pdfium.googlesource.com/pdfium/+/75f84a56fed36111ece82d0ac96e87289622b093/public/fpdfview.h 。
検証: Release、PDF/印刷、UI検証、同一契約書のUI検証が成功。このPCのDLLと生成物一致を確認し導入完了。今回の描画修正はローカル保存。

## 上部を開く・印刷だけに整理（2026-09-14）
利用者依頼で上部のロゴ・airyPDF文字・保存・編集／検索・その他を削除。Windowsタイトルバーのアプリ識別は維持。開くと印刷のツールチップに機能名とCtrl+O／Ctrl+Pを2行で表示。保存はCtrl+S、編集／検索は既存Ctrl+F、タブ閉じるはCtrl+W。全ページ回転メニューを除去し、回転アイコンは表示中のページへ適用。
検証: Releaseビルド・発行、既存UI操作検証に成功。上段が開く・印刷だけの実画像を確認し、このPCのDLLと生成物一致を確認して導入完了。変更はローカル保存。

## 用紙外背景の調整（2026-09-14）
利用者依頼で閲覧背景を #E6EAF0 から少し暗い #BCC3CC に変更。白い用紙との境界を見やすくする。単色、グラデーションなし。Releaseビルド・発行に成功。
このPCのDLLと生成物一致を確認して背景色変更を導入完了。変更はローカル保存。

## 用紙外を黒へ変更（2026-09-14）
利用者の追加希望で閲覧用紙外背景を #BCC3CC から #000000 に変更。Releaseビルド・発行に成功。
黒背景版がこのPCのDLLと生成物一致を確認して導入完了。ローカル保存。

## 背景色を前のグレーへ戻す（2026-09-14）
利用者の指示で黒背景を撤回し、用紙外の色を一つ前の #BCC3CC に戻す。Releaseビルド・発行に成功。
前のグレー背景版のDLLと実インストールの一致を確認し、導入完了。ローカル保存。

## 軽量保存 ＠＠＠（2026-09-14 夕方）
### 完了したこと
- ブラウザ比較で文字描画を見直し、閲覧はFPDF_LCD_TEXTと画面画素に一致する画像サイズへ変更。前の2倍描画・縮小方式は撤回。同じ契約書を原本未変更で表示し、文字の輪郭を画像比較した。
- 上段は開く・印刷の2ボタン。ロゴ、airyPDF文字、保存、編集／検索、その他は除去。カーソルで機能名とCtrl+O／Ctrl+Pを2行表示。
- 保存はCtrl+S、編集／検索はCtrl+F、タブを閉じる操作はCtrl+W。回転は現在ページへ適用。
- 用紙外の背景は黒を試した後、利用者の最終指示でグレー #BCC3CC へ戻した。このPCの固定パスへ導入済み。
- 最新セットアップ: artifacts/release/airyPDF-1.1-20260914-164327.zip。SHA256 A6DF3120749B594D181452D3930E1AF484C2B8FE082C89AD24EBE79DC2E29E36。
- Releaseビルド、既存UI操作、文字描画変更時の印刷回帰、同一PDFのUI検証を実施済み。
- 今回作業で新しい使い捨て補助スクリプトは作っていない。旧版の配布ZIP・表示検証画像・原本等は復旧や比較に必要なので削除しない。
### 次に着手すべき
- 利用者の同じPDFで文字の見え方とグレー背景の最終評価を確認。
- 仲間へ渡す最新版は164327版。GitHub Release v1.1.0のZIPは103303版のままなので、配布Release更新は別の識別子で行う。
### 重要な設計原則・未確定論点
- 閲覧文字の描画変更は印刷描画・寸法を変えない。元が画像のPDFでは元画像の解像度が上限。
- 原本PDF・個人情報・証明書はGitや配布物へ含めない。リポジトリは非公開を維持。
- 新規機能ではなく利用者が希望する最小限のUIへ整理した。PDF互換機能の未対応はREADMEを参照。

## GitHub配布版を最新版へ更新（2026-09-14）
利用者明示依頼で最新164327 ZIPをv1.1.0-20260914として登録しLatestに指定。旧Releaseは保持、非公開を維持。URL: https://github.com/MitsunobuSuhara/airyPDF/releases/tag/v1.1.0-20260914 。GitHub側uploaded・112846890バイト・SHA256 A6DF3120749B594D181452D3930E1AF484C2B8FE082C89AD24EBE79DC2E29E36一致を確認。サンプルPDF未確認として一度審査拒否後、4ページ全文とGit管理の寸法確認PDFとのバイト一致を確認し再審査で許可。古い記録のRelease ZIP旧版という状態は本更新で解消。

## 画面幅ボタンのアイコン化（2026-09-15）
利用者依頼で画面幅に合わせるを左右矢印と両端の線のベクターアイコンへ変更。右端配置を維持。ツールチップは画面幅に合わせて表示、アクセシビリティ名は画面幅に合わせる。既存の倍率操作テストは名前参照へ変更。Releaseビルド・発行に成功。
アイコン版のDLLと実インストールの一致を確認して導入完了。ローカル保存、GitHub Releaseは前日の版。

## 開く・印刷のアイコン化（2026-09-15）
利用者依頼で開くはフォルダー、印刷はプリンターのベクターアイコンへ変更。カーソル時の機能名・Ctrl+O／Ctrl+P表示を維持。Releaseビルド・発行に成功。
実インストールDLLと生成物の一致を確認して導入完了。ローカル保存。

## アイコンを少し柔らかく調整（2026-09-15）
利用者希望で開く・印刷アイコンに丸みと淡い単色を追加。フォルダーは淡黄、プリンターは淡青、プリンターに小さな状態ランプ。機能・ツールチップ・ショートカットは維持。Releaseビルド・発行に成功。
実インストールDLLと生成物一致を確認して導入完了。ローカル保存。

## 開く・印刷アイコンをモノトーンへ（2026-09-15）
利用者の最終指定で淡色を撤回し白・グレー・黒へ変更。丸みのある形、説明とショートカットは維持。Releaseビルド・発行に成功。
モノトーン版の実インストールDLLと生成物一致を確認して導入完了。ローカル保存。

## 正式アイコンを折り紙のツルへ変更（2026-09-15）
利用者がツル案を採用。assets/icons/airyPDF-crane.pngを正式画像とし、exeのICO（16〜256pxの7サイズ）とウィンドウ画像を変更。旧羽根PNGは比較・復旧用に保持。Releaseビルド・発行は警告0／エラー0。32pxの実ICOを目視確認。このPCの導入DLLと発行DLL一致を確認。最新セットアップ: artifacts/release/airyPDF-1.1-20260915-070454.zip、SHA256 D8E5A2A3CE1E19DE4BBA1F5BC77E8992E495565903BA4126EB9CCEB887B6ADB7。GitHub Releaseの配布ZIPは9/14版のまま。


## 左上のツルの視認性改善（2026-09-15）
16pxで首と折り目がつぶれるため、16/24/32pxは太い首と大きな面の専用図形をmake-icon.ps1で生成。大きいサイズは採用原本を維持。WPFのIconをPNGから複数サイズICOへ変更。16px画像を拡大して輪郭を確認、Releaseビルド警告0／エラー0、このPCの導入DLL一致。


## タスクバー・エクスプローラーのアイコン統一（2026-09-15）
利用者依頼で16〜64pxを見やすいツル専用図形へ統一。airyPDF-crane-v2.icoをアプリ直下へ同梱し、デスクトップ・スタートメニュー・既存のairyPDFピン留めリンク・Applications/airyPDF.exe/DefaultIcon・アンインストール表示が参照。登録後に関連付け変更通知。新たなピン留めや他アプリの関連付けは変更しない。確認時PDF UserChoiceはMSEdgePDFであり、PDFファイル自体のアイコンは既定アプリに従う。Releaseビルド警告0／エラー0、導入DLL一致、デスクトップとスタートメニューの専用ICO参照・レジストリ登録を照合。配布ZIP: artifacts/release/airyPDF-1.1-20260915-070936.zip、SHA256 CB4307E2DD9234A1B0611FBE60BE4AC23EA5B9BDDF8AAF3754068A5D294FCF05。


## 軽量Markdown表示（2026-09-15）
利用者希望で.md/.markdownをairyPDF内の読み物表示で開ける機能を追加。ブラウザエンジンや追加パッケージは使わずWPF標準FlowDocumentのみ。見出し、太字、箇条書き、引用、区切り線に対応し、選択・スクロール可。PDF処理には追加負荷なし。開くダイアログ、ドラッグ＆ドロップ、コマンドラインに対応。実画面をartifacts/markdown-window.pngで目視。既存UIテストのCanvasをstringへ直接castする不具合も修正し、PDF・印刷テストとMarkdown画面テスト成功。配布ZIP: artifacts/release/airyPDF-1.1-20260915-162521.zip、SHA256 E682BE0E5E5C2CED9DB886AF53BA066D01EFAD43822495CBAD789292905AC1A2。共有用Markdown: artifacts/share/airyPDF_AIとの会話から生まれたPDFアプリ.md。GitHub pushは自動承認で宛先所有者未確認として拒否されたためローカルcommitまで。


## MarkdownをPDFと同じタブへ統合（2026-09-15）
利用者が別ウィンドウ表示を望まなかったため、MarkdownをMainWindowのTabControl内へ統合。PDFと.md/.markdownを同じタブ列で切替。Markdownタブは白背景の読み取り専用本文だけを表示し、PDF用のページ・倍率・回転ツールバーと印刷ボタンを隠す。PDFタブへ戻ると復帰。ブラウザ・外部パッケージなし。閉じる処理はPDFとMarkdownの型を安全に分け、Ctrl+W対応。PDF/印刷・UI・Markdown同一タブ表示テスト成功、実画面を目視。このPCへ導入DLL一致。配布ZIP: artifacts/release/airyPDF-1.1-20260915-163449.zip、SHA256 AE7DF723561B8B7939A8E879E621E0483EEC29D016844E4FE7722CDCD1AF4EF7。


## Markdownのホイールスクロール高速化（2026-09-15）
利用者が長文Markdownでホイールスクロールが遅いと指摘。Windows標準の行単位移動を上書きし、1ノッチ180pxの一定移動へ変更。ブラウザや追加部品なし、PDFスクロールは変更なし。Markdownは読むだけの方針を維持し、印刷ボタンは非表示。印刷はレイアウトの確定したPDF版を使用。80段落の長文UIテストで1ノッチ170px以上、PDF/印刷・UI全検証成功。このPCへ導入DLL一致。利用者のDesktop/airyPDF一発作成プロンプト.mdを更新版で起動。配布ZIP: artifacts/release/airyPDF-1.1-20260915-164426.zip、SHA256 5F2B1563A4C0A358214025583E5A5D3035C65508B7A4363EC90BE6046D231152。


## AiryReaderへ完全統一（2026-09-15）
利用者の明示指示で正式表記を `AiryReader` に統一。実行ファイルは `C:\Program Files\AiryReader\AiryReader.exe`、デスクトップ／スタートメニューは `AiryReader.lnk`、設定は `LocalAppData\AiryReader`、配布ZIP・共有資料・プロジェクト・名前空間・アイコンも同じ表記。旧 `C:\Program Files\airyPDF` は新DLLとの一致確認後に削除済み。旧既定アプリ設定を壊さないため、旧名を検出して新実行ファイルへ移す互換処理だけを内部に保持する。
PDF、Markdown、TXT、HTML、JPEG、PNG、TIFF、BMPの同一タブ閲覧を維持。CSV・Word・JSON・XMLは対象外。Release、PDF／印刷、UI、Markdown／TXT／HTML／画像の全テストは警告0・エラー0。寸法確認PDFの見出しもAiryReaderで再生成。共有資料は `artifacts/share/AiryReader_AIとの会話から生まれたPDFアプリ.{md,docx,pdf}` に統一。
最終配布ZIP: `artifacts/release/AiryReader-1.2-20260915-174551.zip`（112516002バイト、SHA256 `8935C1F27298C2BFB80AC9D3608E38ED0F4B379C460961B802C9B77786A5AB1E`）。非公開GitHubも `MitsunobuSuhara/AiryReader` へ改名し、originを新URLへ更新。 GitHub Release `v1.2.0`（https://github.com/MitsunobuSuhara/AiryReader/releases/tag/v1.2.0）へ同ZIPを登録し、GitHub側の容量・SHA256一致とuploadedを確認。
## 画像・文章の基本操作と紹介文改稿（2026-09-15）
利用者依頼でAiryReader 1.2.1へ更新。JPEG/PNG/TIFF/BMPはCtrl＋ホイール、＋／－、倍率入力、Ctrl＋0、画面内フィット、左右90度回転、ダブルクリックによる100％／フィット切替に対応。通常ホイールはスクロールを維持。Markdown/TXT/HTMLはCtrl＋ホイール、＋／－、倍率入力、Ctrl＋0で文字倍率を変更し、Ctrl＋Fで検索欄、全一致強調、前／次移動、Esc終了に対応。選択・コピー、通常スクロールを維持。PDF機能と印刷経路は変更なし。
Forest Cruise、TerraTrace、fc-office、hauldesk、Forest Cruise Hokkaido等のREADME/AGENTSを確認。共有文書を「コード経験はないが、AIとの会話で複数の実務アプリを作り、業務・規程・実機・結果を判断して改善する経験は積んでいる」という正確な内容へ改稿。約1日でAiryReaderが形になった速さを、過去の試行経験とAIの実装速度の組合せとして説明。Markdown/Word/PDFを更新。PDFは強制改ページを整理して6ページ、全ページ文字量・描画範囲を検査。
Release・UI・PDF・印刷・画像・文章操作の自動テスト成功。配布ZIP: `artifacts/release/AiryReader-1.2.1-20260915-181803.zip`（112527074バイト）、SHA256 `D69B8A51C59042A1BBAFA6F0991B4F692CC99DC99416067ACA7283B4290810FC`。GitHub ReleaseへのZIP登録は自動承認審査で外部共有の明示承認不足として停止。
## 共有文書から個別アプリ名を除去（2026-09-15）
利用者のPDFコメントにより、共有文書では過去に作った具体的なアプリ名を出さない方針へ変更。「現場入力」「測量」「帳票作成」「GIS」「地域別規程対応」という仕事内容だけを示し、コードは書かないがAIとの会話で複数の実務ツールを作って試す経験がある、という主旨は維持。Markdown、Word、PDFを再生成し、PDF全文に個別名がないこと、6ページすべてに本文があり描画範囲が正常なことを検査。
## Windows既定アプリ登録（2026-09-15）
利用者依頼によりAiryReader 1.2.2へ更新。WindowsのRegisteredApplications／Capabilities／FileAssociationsと専用ProgIDを登録し、「設定」→「アプリ」→「既定のアプリ」→「AiryReader」にPDF、Markdown、TXT、HTML、JPEG、PNG、TIFF、BMPの12拡張子が表示されるようにした。Program Filesへ1.2.2を導入し、登録値を確認。Windowsの保護仕様により最終的な既定選択は利用者が設定画面で行う。Release・UI・PDF・印刷テスト成功、警告0・エラー0。配布ZIP: `artifacts/release/AiryReader-1.2.2-20260915-185058.zip`、SHA256 `EBAC6035F4AE94DB920C6E2F8D7CE4517129B266A632355F3281E1B5A9C28630`。

## HTML対応を対象外へ変更（2026-09-15）
利用者がHTMLは通常ブラウザで閲覧すると整理したため、AiryReader 1.2.3でHTML/HTMの読込、ファイル選択、ドラッグ＆ドロップ、案内、自己検証を削除。既存1.2.2のCapabilities、SupportedTypes、OpenWithProgidsに残るHTML/HTM登録もインストール時に除去する。HTML処理は小さく、削除による起動速度・容量の差はほぼないが、対応範囲と既定アプリ選択が明確になった。Program Filesへ導入し、HTML/HTM登録なし、PDF・Markdown・TXT・主要画像10拡張子の登録を確認。全テスト成功、警告0・エラー0。配布ZIP: `artifacts/release/AiryReader-1.2.3-20260915-190336.zip`、SHA256 `D7313A50DDF182616CA094F49BDB7C22F8AD1687C5C823871E62C0820DA3462C`。

## TXT直接編集（2026-09-15）
利用者依頼によりAiryReader 1.2.4でTXTを閲覧専用から直接編集へ変更。TXTタブでは編集欄と保存ボタンを表示し、Ctrl+Sで元ファイルへ上書き保存する。UTF-8とShift-JISを読込時に判定し、元の文字コードを保って保存。変更中はタブ名に`*`を表示し、タブ／ウィンドウを閉じる際に保存・破棄・取消を確認する。Markdownは閲覧専用を維持。TXTの追記・実ファイル保存を含む全テスト成功、警告0・エラー0。Program Filesへ1.2.4導入済み。配布ZIP: `artifacts/release/AiryReader-1.2.4-20260915-191617.zip`、SHA256 `96108EEACDBC6AF229AD4C0B1355BF7B59C0194EA2C2600F45E3EF6C448CA502`。

## 単体起動をメモ帳化（2026-09-15）
利用者依頼によりAiryReader 1.2.5では、ファイル指定なしで単体起動すると編集可能な`無題.txt`タブを自動作成する。Ctrl+Nで新しいメモ、Ctrl+Sで初回は保存先選択・以降上書き、Ctrl+Shift+Sで別名保存。未保存マークと終了確認、UTF-8/Shift-JIS保持は継続。ファイル指定起動時は余分な白紙タブを作らない。白紙作成・保存を含む全テスト成功、警告0・エラー0。Program Filesへ1.2.5導入済み。配布ZIP: `artifacts/release/AiryReader-1.2.5-20260915-192358.zip`、SHA256 `FA8F92F47B43EC6D8A2D1D1D620E3414B68F8DEFDDD7EB0EFC67963B66E8DC87`。

## メモのタブ追加・終了操作（2026-09-15）
利用者依頼によりAiryReader 1.2.6でタブ列に`＋`と`×`を追加。＋クリックまたはCtrl+Tで編集可能な無題TXTタブを追加し、×クリックまたは既存のCtrl+Wで現在タブを閉じる。両ボタンはカーソルを合わせるとショートカットを表示する。Ctrl+Nも新規メモとして維持。新規タブ増加と既存ズームボタンの識別をUIテストで確認し、全テスト成功、警告0・エラー0。Program Filesへ1.2.6導入済み。配布ZIP: `artifacts/release/AiryReader-1.2.6-20260915-192936.zip`、SHA256 `CC03A71600A0B56B86B5947CBFDB8059E200E59308505CB29A4B438020C9F1B5`。

## タブ内の閉じるボタン（2026-09-15）
利用者の画面指摘によりAiryReader 1.2.7で画面右端の共通×を廃止し、各タブ名の中に小さな×を内蔵。クリックするとそのタブを閉じ、未保存TXT/PDFの確認も既存どおり行う。＋は画面右端固定から最後のタブ直後へ移し、一般的なタブUIに変更。各×はCtrl+W、＋はCtrl+Tのツールチップを表示。タブ内×の存在、新規メモ、全既存機能のテスト成功、警告0・エラー0。Program Filesへ1.2.7導入済み。配布ZIP: `artifacts/release/AiryReader-1.2.7-20260915-193325.zip`、SHA256 `C1AC0574AA458E2CD67B9BD6526147F60DBD03257F048F3A499B410540B62DD5`。

## シンプルなメモ帳代替（2026-09-15）
利用者はWindowsメモ帳の役割をAiryReaderへ移しつつ、画面をシンプルに保つ方針。AiryReader 1.2.8でTXTのCtrl+F検索を編集欄へ正式対応し、F3で次、Shift+F3で前へ移動。TextBox標準のCtrl+Z/Y、切り取り、コピー、貼り付け、全選択を利用し、専用ボタンは増やさない。新規・タブ・保存・別名保存・未保存確認は既存機能を継続。TXT検索を含む全テスト成功、警告0・エラー0。Program Filesへ1.2.8導入済みで、AiryReaderのWindows既定アプリ画面を表示。`.txt`の最終選択はWindowsの保護仕様により利用者操作。配布ZIP: `artifacts/release/AiryReader-1.2.8-20260915-193707.zip`、SHA256 `E96E2695F05D63A5FD6D88D7DD878C5A5A57DD1E602FC1FF853FDE10FD73FCB0`。

## Markdown既定アプリ登録の修正（2026-09-15）
利用者環境で.md/.markdownのアプリ選択に「一度だけ」しか出ない報告を受け、AiryReader 1.2.9で文書共通・画像共通のProgIDから拡張子ごとの専用ProgIDへ変更。`.md=AiryReader.md`、`.markdown=AiryReader.markdown`など10形式を独立登録し、CapabilitiesとOpenWithProgidsを一致させた。旧共通ProgIDは既存設定の互換用に残すが、OpenWith候補からは外す。MicrosoftのDefault Programs登録仕様に沿う構成。全テスト成功、警告0・エラー0。Program Filesへ導入し登録値を実機確認、既定アプリ画面を再表示。最終選択は利用者操作。配布ZIP: `artifacts/release/AiryReader-1.2.9-20260915-194947.zip`、SHA256 `6FA66F88D7D4F3306B49F89B6DF2ED1C73F3FB489E8A0C05B07FF2CFDAEA9CCF`。

## 背景透明・白い鶴アイコン（2026-09-15）
利用者が候補を採用し「全部に反映」と指示。左右反転済みスクリーンショットは白い角丸パネル外側に黒い余白を含んでいたため、外側と白背景を除去して鶴だけを抽出。1024角透明キャンバス中央へ白一色のシンボルとして配置し、`assets/icons/AiryReader-symbol-white-transparent.png`へ保存。旧アイコンは残す。16/24/32/48/64/128/256pxすべて同じ透明素材からICOを生成するようmake-iconを変更し、旧小サイズ用黒背景描画を削除。紹介資料用には濃色背景プレビューを使用。AiryReader 1.3.0として全テスト成功、警告0・エラー0。Program Filesへ導入し、アイコンキャッシュ更新通知済み。配布ZIP: `artifacts/release/AiryReader-1.3.0-20260915-200404.zip`、SHA256 `B9BDA36EA71D8FBDBE88294E0D18331535959C0C8B43981913DB43AA8CD3C613`。

## 黒いタイトルバー・タブ操作性・アイコン寸法（2026-09-15）
利用者のスクリーンショット確認によりAiryReader 1.3.1へ更新。Windows標準タイトルバーへDWMのダークモードを適用し、背景透明・白い鶴を見やすくした。タブ高さを42px、文字15pxへ拡大し、各タブ内×を30×28px・19px文字にして選択しやすくした。＋も42px高でタブ直後に配置。タスクバーの比較画像では鶴が周囲より小さく見えたため、16～64pxのICOだけ元素材比約30％拡大し、128/256pxと資料画像は維持。全テスト成功、警告0・エラー0。Program Filesへ導入・アイコンキャッシュ更新済み。最終配布ZIP: `artifacts/release/AiryReader-1.3.1-20260915-201450.zip`、SHA256 `A026184B4DBC3210438E62FCBC6E02157D3279A189E426531A9AB285CE2AEA8A`。

## AiryReader 1.4 安全性・Windows統合修正（2026-09-16）

利用者が問題点調査で挙げた10項目を修正。TXTはタブごとに編集中本文を保持し、別タブの内容を誤保存しない。保存は同一フォルダの一時ファイルから置換し、外部変更はSHA-256比較で上書き前に確認する。UTF-8/Shift-JISに加えてBOM付きUTF-16 LE/BE、UTF-32 LE/BEを読める。TXTはCtrl+PからWindows印刷ダイアログで印刷可能。

文章は64MB、画像は256MB・2億画素を上限とし、画像は本体展開前に画素数を検査。タブ列を横スクロール対応。配布ICOは透明な白い鶴の内側デザインを維持しつつ濃色円を敷き、明るい背景での判別性を確保。AiryReader登録時に旧Applications/airyPDF.exeと旧ProgIDを削除する。Windows既定アプリの最終選択はOS保護仕様により利用者操作だが、PDF/MD/Markdown/TXT/画像のCapabilitiesと拡張子別ProgID登録を実機確認。

複数未保存TXTの切替・個別保存、安全な置換、UTF-16、外部変更、TXT印刷、タブ横スクロールを回帰テストへ追加。全PDF・印刷・UIテスト合格、ビルド警告0・エラー0。Program Files導入版 `1.4.0+2853f0c5065735548e8190c4d946e9189040f16f` を確認。配布ZIP: `artifacts/release/AiryReader-1.4.0-20260916-042803.zip`（112522607バイト）、SHA256 `1A4D8442200850D4852ACDA5655C0D6595FEBCB1FDEA4B504F0AFA9E978097A3`。

## 紹介PDF更新・画像の単一ウィンドウ化（2026-09-16）

`artifacts/share/AiryReader_AIとの会話から生まれたPDFアプリ.pdf` をAiryReader 1.4の現状へ更新。複数TXTタブ、安全な置換保存、外部変更警告、UTF-16/32、TXT印刷、大容量保護、タブ横移動、Windows登録整理を「現在のAiryReader」へ追加。PDFは6ページ、必須文言、全文字のページ内座標、全ページPNGの余白を検査。元のMarkdownとDOCXも同内容へ更新（成果物はGit対象外、生成元スクリプトをGit保存）。

AiryReader 1.4.1では通常起動を1プロセスに集約。エクスプローラーから後続のPDF・PNG・JPEG等を開くと、同じWindows利用者の起動中AiryReaderへ名前付きパイプでパスを渡し、既存ウィンドウのタブへ追加する。全既存テスト合格、ビルド警告0・エラー0。Program Files版 `1.4.1+ee3ed5ec4fa99f4ed39e090a8c965582399c6153` を確認。PNGとJPEGを別起動する実機試験で、後続プロセス終了・AiryReaderプロセス1・画面上タブ2を確認。配布ZIP: `artifacts/release/AiryReader-1.4.1-20260916-051240.zip`、SHA256 `CDCD4C61F94B971E74321808DC06AF7169664DA4C0510E643A2A348F30572A8F`。

## 制作記録の時間表現を補正（2026-09-16）
利用者の実感に合わせ、共有資料の時系列を「昨日か一昨日の朝にPDFリーダーの枠組みが完成し、昨日は細かな改善やメモ機能を追加」へ変更。厳密な経過時間の列挙を本文から外し、実際に会話していた時間は肌感覚で合計2〜3時間という説明を維持。共有PDFを6ページで再生成し、時系列の文言と文字情報を確認した。

## 制作開始日の表現を確定（2026-09-16）
Git記録に基づき、共有資料の曖昧な相対表現を、制作開始日の「9月13日の朝」へ修正。PDFリーダーの基本的な枠組みがこの朝にでき、細かな改善やメモ機能はその後に追加した流れへ整理し、共有PDFを再生成した。

## 制作記録へ9月13日朝の具体的な流れを追加（2026-09-16）
Git履歴に基づき、8:23の要件整理、8:54のPDF閲覧・原寸印刷プレビュー試作、9:24のデスクトップ化と実機確認準備、9:51の複数ページ連続スクロール、9:59の起動先修正を、共有資料へ時系列の表として追加。この朝にPDF閲覧・連続スクロール・原寸印刷プレビュー・デスクトップ起動まで形になり、その後に細かな改善やメモ機能を追加した流れを明記。共有PDFは6ページ、全時刻の文字情報とページ内配置を確認した。

## 制作時間の重複表現を整理（2026-09-16）
共有資料で「肌感覚では合計2〜3時間」が表紙・制作経過・結びに重複していたため、制作経過の本文1か所だけに整理。表紙は概要、結びは短い会話の積み重ねという表現へ変更し、6ページのPDFを再生成した。

## 制作記録の操作説明を具体化（2026-09-16）
共有資料の「幅に合わせるは『画面幅に合わせる』に」は開発当事者以外に背景が伝わらないため、「PDFを画面の横幅いっぱいに表示するボタンは、意味が分かる名前にしてください」へ変更。6ページの共有PDFを再生成した。

## 紹介資料から技術仕様を分離（2026-09-16）
共有資料の「現在のAiryReader」に並んでいた文字コード、安全な置換保存、外部変更警告、大容量保護などの技術説明を削除。PDFの軽い閲覧と原寸印刷、複数ページ、記入・検索、Markdown・画像・テキスト、メモ帳利用という、一般の読者が用途を理解できる説明へ変更。配布時の技術情報はREADME等の別資料で扱う方針とし、6ページの共有PDFを再生成した。

## 制作記録の時刻表を削除（2026-09-16）
共有資料から9月13日朝の8:23〜9:59の詳しい作業表を削除。「朝に欲しいものをAIへ話し、その日の朝のうちに基本形を試せた」「コードを書かず、普段の言葉で感想を返した」「合計2〜3時間ほどの会話で使えるアプリが形になった」という要点へ集約。簡単に作り始められることが読者へ伝わる構成にし、6ページの共有PDFを再生成した。

## 制作記録の会話背景と改行を改善（2026-09-16）
共有資料の「はい。新しいPDFは100％で始めたいです…」は背景がなく、表内の折り返しも不自然だったため、最初の試作を触って表示倍率とページ移動への希望が出たことを本文で補足。発言は「PDFは最初から100％で表示」「何ページもある資料は縦に続けてスクロールして読みたい」へ整理し、6ページのPDFを再生成した。

## 制作記録の会話例をページ内で一体化（2026-09-16）
利用者のスクリーンショットで、会話例の質問とAI回答が2ページ目末尾、続く利用者の返答だけが3ページ目先頭へ送られていたことを確認。会話例の前で明示的に改ページし、3つの発言と背景説明が同じページで続けて読めるように変更。全体は6ページを維持して共有PDFを再生成した。

## 制作記録からPC持参の記述を削除（2026-09-16）
利用者の注釈により、「会社の複合機で印刷チェックをします。このPCを持って行きます」を「会社の複合機で印刷を確認します」へ変更。通常は自分のPCを会社へ持参しないため、制作の要点に関係しない個別事情を紹介資料から外した。6ページの共有PDFを再生成した。

## 制作記録から規程への言及を削除（2026-09-16）
利用者の注釈により、「業務の流れ、規程、使い方、結果の良し悪しを伝えました」を「使い方や、実際に試した結果の良し悪しを伝えました」へ変更。紹介に不要な規程への言及を外し、6ページの共有PDFを再生成した。

## 公開リポジトリ化と1.4.2 Release（2026-09-16）

利用者の明示依頼でGitHub MitsunobuSuhara/AiryReader をPUBLICへ変更。公開前に全Git履歴を秘密鍵・APIキー・証明書・個人資料・社内IP・ローカル個人パスのパターンで監査し、実在する秘密情報は検出されなかった。テスト内の固定文字列 secret とパスワード入力処理の変数名のみ確認。ルートライセンスは未設定のため、公開は閲覧・ダウンロードを可能にするもので、ソース再利用許諾は別途付与していない。

`build.ps1 -Test -Publish` はビルド警告0・エラー0、PDF・印刷・UIテスト成功。配布ZIP AiryReader-1.4.2-20260916-070624.zip（112,539,823バイト、SHA256 2F0E3E0D9842750FD35432C5C3579927C4F25036CE7B85E1D2AC7D10B046F49C）と紹介PDF AiryReader-Story.pdf（SHA256 1D2CA19B3A0F96B50701B9CED6B56FC78AC463AE6C9E11517055D815697606BA）をGitHub Release `v1.4.2`へ登録し、GitHub側digest一致・uploadedを確認。READMEへLatest Releaseからのダウンロード案内を追加。ローカル開発フォルダは C:\dev\AiryReader へ統一する。

## 操作列の2段化・表示幅切替・回転リセット（2026-09-16）

AiryReader 1.4.3で、独立していた「開く・印刷」の行と下部の状態表示を通常画面から外し、最上段の操作列へ統合。操作列を1行目、タブ列を2行目にして、通常時はそれより下をPDF・Markdown・画像・TXTの表示へ使う構成にした。検索を開いた時だけ検索欄が一時的に追加される。

「画面幅に合わせて表示」は、現在が幅合わせならもう一度押すと100％へ戻るトグルに変更。画像でも同様に動作する。左右90度回転の横に「リセット」を追加し、画像は表示回転を0度へ、PDFは現在ページの回転を0度へ戻せる。PDF内部処理テスト、画面操作テストともに成功（警告0・エラー0）。

GitHubリポジトリ `MitsunobuSuhara/AiryReader` は利用者の指示により現在PRIVATE。公開時の記録は履歴として残すが、現時点ではアクセス権を持つ人だけが閲覧・ダウンロードできる。
## 保存・回転リセットのアイコン化（2026-09-16）

利用者の画面指摘に合わせ、通常の操作ボタンは文字よりアイコンを優先する方針を反映。TXTの「保存」をフロッピーディスク形アイコンへ、回転の「リセット」を回転矢印アイコンへ変更した。説明とショートカットはカーソルを合わせた時のツールチップで表示する。ビルド警告0・エラー0、画面操作テスト成功。
## MIT License化と公開配布の簡略化（2026-09-16）

利用者の明示依頼により、AiryReaderをMIT Licenseでオープンソース化する。利用・複製・改変・再配布・商用利用を、著作権表示とライセンス文を残す条件で許可する。GitHubリポジトリは公開へ変更する方針。

配布ZIPは展開後に `AiryReaderをセットアップ.cmd` をダブルクリックするだけに整理し、`はじめにお読みください.txt` を同梱。管理者確認後、デスクトップとスタートメニューへ登録される。README冒頭も同じ3ステップへ整理した。
## 紹介PDFをGitHub公開対象から除去（2026-09-16）

利用者の明示指示により、内々用の `docs/AiryReader_AIとの会話から生まれたPDFアプリ.pdf` をGit管理から外し、`.gitignore` で再登録を防止。ローカルのPDF実体は保持する。GitHub Release `v1.4.2` の `AiryReader-Story.pdf` も削除した。

公開済みコミットからも紹介PDFを取り除くため、利用者の明示承認後にmainと全タグの履歴を再構成して強制pushした。GitHubのdocs一覧は `VALIDATION.md` のみ、Release添付はアプリZIPのみであることを確認。公開済みURL・フォーク・キャッシュは外部に残る可能性があり、完全な回収はできない。
## GitHub配布説明を簡潔化・1.4.3 Release（2026-09-16）

利用者の指示により、公開GitHubのREADMEを初心者向けの最小構成へ整理。ダウンロード、ZIP展開、`AiryReaderをセットアップ.cmd` のダブルクリック、Windows確認の4手順、短い機能一覧、MIT Licenseだけを掲載する。詳しい運用説明は別途メールで送る方針。

GitHub Release `v1.4.3` を公開。添付は `AiryReader-1.4.3-20260916-104817.zip` のみで、ZIPには日本語名のセットアップ実行ファイル、最初に読む案内、README、LICENSEを同梱。Release URL: https://github.com/MitsunobuSuhara/AiryReader/releases/tag/v1.4.3
## 軽量保存 @@@（2026-09-16 16:30）

### 完了したこと

- AiryReader 1.4.4を公開。TXT／Markdown Sourceの折り返しを`Alt＋Z`で切替でき、初期状態はOFF、タブごとに保持する。
- 開いたPDF・Markdown・TXT・画像をWindowsの「最近使ったファイル」へ登録する。Windows側の履歴表示設定が無効な場合は表示されない。
- MarkdownはPreview／Source切替、Source編集・安全保存、閉じたタブは`Ctrl＋Shift＋T`で復元できる。
- ビルド警告0・エラー0、自己テスト・UIテスト成功。Program Filesへ実行環境同梱版を導入済み。
- GitHub Release `v1.4.4`を最新版として公開。ZIPは`AiryReader-1.4.4-20260916-162201.zip`、SHA256は`FCF4BE043205B19A3447A7E7CCB7A83C3088B4E440482D97191A0B9FB44317B8`。
- Release ZIPに内々用の紹介PDFは含めていない。アプリ機能用の`app/Samples/print-check.pdf`のみ同梱。
- 今回使用した`tmp`／`artifacts/tmp`は存在せず、削除した一時ファイルはない。配布ZIPは成果物のため保持。

### 次に着手すべき

1. 利用者が希望する場合、ステータス表示の`Ln`／`Col`を初心者向けに`行`／`列`へ変更する。
2. タスクバー右クリックの「最近使ったファイル」がWindowsの設定下で表示されることを実画面で確認する。
3. 次の機能変更時はバージョンを1.4.5以降へ上げ、自己テスト・UIテスト・実行環境同梱ZIP・Releaseの順で更新する。

### 重要な設計原則・未確定論点

- 起動速度と軽さを優先し、起動時の履歴走査は行わない。履歴登録はファイルを正常に開いた時だけ行う。
- 紹介PDFは内々用で、Git・GitHub Release・配布ZIPへ含めない。
- 利用者へ.NETの別途導入を求めず、Releaseは`--self-contained true`のWindows x64版を配布する。
- GitHubは簡潔な案内に留め、管理署担当者向けの詳しい説明は別途メールで行う。
## AiryViewへの正式名称変更（2026-09-17）

利用者の正式決定により、AiryReaderをAiryView（エアリービュー）へ全面変更。表示名、namespace、プロジェクト名、実行ファイル、アイコン名、Program Files、Windows登録、ショートカット、設定保存先、補助実行ファイル、配布ZIP、README、紹介PDFをAiryViewへ統一し、版を2.0.0とした。旧AiryReader／airyPDFの設定は初回読込対象として維持し、旧インストール・登録・ショートカットは利用者の明示承認後に削除済み。旧名文字列は移行互換処理と履歴説明にのみ残す。

全ビルド・自己テスト・UIテスト・PDF補助機能テストに合格。寸法確認PDFはA4縦、A4横、A3横、A4縦の実寸を維持。紹介PDFは6ページを再生成し、全ページ描画・旧名不在・文字範囲を確認。GitHubリポジトリは `MitsunobuSuhara/AiryView` へ、正式なローカル開発場所は `C:\dev\AiryView` へ変更済み。GitHub Release `v2.0.0` には `AiryView-2.0.0-20260917-051957.zip` を公開し、GitHub側のSHA256がローカル生成物と一致することを確認した。

旧作業場所 `C:\dev\airyPDF` には未コミットの変更が残っているため、データ保護のため削除・改名していない。現在のCodexタスクが使用中の `C:\dev\AiryReader` も終了まではWindowsにより完全改名できないため、内容を `C:\dev\AiryView` へ統合済み。今後の作業は必ず `C:\dev\AiryView` を使用する。
## 2.0.14 表示速度改善（2026-09-24）
PDF寸法取得の本文解析・繰返し取得、WebP/SVGのPNG中間変換、文章の二重読込とTXTの不要なFlowDocument作成を削減。ICC色補正と原本解像度は維持。合成データのウォーム計測でPDF100ページ206→32ms、WebP244→20ms、SVG274→30ms。JPEG・文章等は明確な改善なし。全形式が同じ倍率で速くなるとは扱わない。
詳細・全形式の数値・計測範囲はdocs/PERFORMANCE-2.0.14.md。自己テスト42件・UI149件・画素一致等の追加検証成功。2.0.14の.NET同梱publish完了。NuGet脆弱性情報取得のNU1900警告あり。起動先への反映はこの節の追記で確認する。既存のscripts/create-airyview-story.pyの変更は今回の速度改善と無関係のため保持。
2026-09-24 07:06 管理者確認の再表示後、2.0.14のインストール成功。C:\Program Files\AiryView\AiryView.dllのバージョン2.0.14.0、artifacts/appとのSHA256一致、install-result.txtの更新を確認。上記の管理者確認待ちは解消済み。

### 軽量保存 @@@（2026-09-24）
- 当セッションの速度改善・テスト・計測記録をmainへ保存する。既存のscripts/create-airyview-story.pyの変更は対象外として保持。
- tmp/pdfsは当セッション以前のもののため保持。今回のログ・比較CSV・合成テストデータは検証証跡としてartifactsへ保持し、削除した一時ファイルはない。
- 次は実際の利用ファイルで体感を確認。追加改善は遅い形式・ファイルを特定して計測してから行う。全形式の一律高速化・コールド起動の改善は未確認。
- GitHub Releaseの追加公開は実施していない。共通引継ぎはdev-config/handover/AiryView/2026-09-24.md。

## 2.0.15 初期表示停止対策（2026-09-26）

利用者は「たまに枠だけ出て中身が空白、マウス操作で表示される」と報告。通常・表示前読込・最小化復帰・同一ファイル再表示の入力なしテストでは元の実症状は再現しなかった。一方、Windowsの履歴登録がUIスレッド上で同期実行され、Shellの遅延で表示完了まで止まる経路を確認。1.5秒の遅延を注入した回帰テストで旧同期処理が失敗し、修正後成功した。実際の利用者の症状と同一原因と断定しない。

- RecentFileHistoryで履歴登録を専用STAワーカーへ移動。順番を維持し、登録失敗でも後続を処理。待ちキュー上限64、空になったらワーカーは終了、アプリ終了を妨げない。
- 外部起動のパイプ受信をUIから分離。部分送信の待ちを5秒で解放、終了時の受信もキャンセル。画面復帰後にファイルを開く。
- 初期画像フィットをContextIdle待ちからLoaded優先度へ変更。Loaded・最小化解除・未完了画像のSizeChangedで再試行する。PDFもロード完了・復帰時に再描画し、未ロード・最小化中の不適切な描画を避ける。画面終了時に描画タイマーも停止。
- --startup-testを追加しbuild.ps1 -Testに組込み。画面更新の強制や入力なしのPDF/PNG表示、WPF描画フレーム、Shell遅延・失敗、途中切断した外部受信後の復帰を確認。既存のPDF・印刷・UIテストも成功。スクリーンショットartifacts/startup-pdf-window.pngを目視確認。
- Release・.NET同梱publish成功。NuGet脆弱性情報取得のNU1900警告のみ。生成物はartifacts/app、検証記録はartifacts/startup-test-results.txtとstartup-final-build.log。
- 次は2.0.15のインストール反映確認と実利用での再発有無確認。GPU描画等による別原因までは今回の合成テストでは否定できない。既存scripts/create-airyview-story.pyの変更は対象外として保持。
- WPF JumpList.ApplyはSTAを要求するがApplicationへの取付けは必須でないことを公式ソースで確認: https://github.com/dotnet/wpf/blob/main/src/Microsoft.DotNet.Wpf/src/PresentationFramework/System/Windows/Shell/JumpList.cs
2026-09-26 18:27 インストール完了。C:\Program Files\AiryView\AiryView.dllは2.0.15.0、artifacts/appとのSHA256一致、install-result.txtの更新を確認。既存の起動プロセスは通常終了要求で閉じ、強制終了はしていない。検証合計213件（自己42・UI149・起動22）。今回の変更は未commit。

## GitHub Release v2.0.15と受け渡しエラーの復旧（2026-09-26）

- 利用者の明示依頼でGitHubへcommit eb104baをpushし、Release v2.0.15をLatestとして公開。URL: https://github.com/MitsunobuSuhara/AiryView/releases/tag/v2.0.15
- 配布ZIP: AiryView-2.0.15-20260926-182852.zip、184003380 bytes。SHA256: A01DB3B6B5F663F39344424CAF34A01CDD9723429D1518BDB7E352D9C1B7DC62。GitHub側uploaded・digest一致・タグの対象eb104baafe9da18a480aef4d783874fa1bbe0116を確認。収録DLLは実際に動作したProgram Files版と一致。収録PDFはSamples/print-check.pdfだけ。
- 更新後、こちらが通常のシェル経由で起動したアプリが作業用Windowsユーザーの画面なしプロセスとして残り、利用者の通常ユーザーから受け渡せないエラーを引き起こした。対象プロセスの所有者・実行パス・ファイル引数なしを確認し、管理者実行でそのプロセスだけを解消。元の「たまに空白」の報告とは区別する。
- 通常ユーザーで導入済み2.0.15を開き直し、利用者のPDF表示を実画面で確認。同一ファイルの再受信は終了コード0、プロセスは通常ユーザーの1個のみで成功。今後、実機確認用アプリは作業用ユーザーではなく通常ユーザーで起動する。
- ユーザー・セッション別の起動名を試作したが、その追加ビルドの実行がWindowsのアプリ制御ポリシーで拒否された。制御設定の変更・回避はしていない。追加変更は採用せずソースをeb104baへ戻し、公開対象は既に実機動作確認済みのZIPとした。artifacts/appも公開ZIPの元フォルダから同じ版へ戻した。
- 追加ビルドを検証したbin/objは次回ビルドで更新される。復旧ログと未採用ビルドの記録はartifacts/repair-install-*.txt、startup-scope-*.log。これらは配布ZIPへ含めない。既存scripts/create-airyview-story.pyの未commit変更は保持。

## 2.0.16 ファイル起動時の案内画面を省略（2026-09-26）

- 利用者の希望に合わせ、アプリ単独起動は鶴付き案内を維持し、ファイル指定起動はShow前から案内を非表示にする。ファイル起動では案内用アイコンのデコードも行わず、必要になった時だけ読み込む。
- 起動済みアプリへのファイル受信は画面復帰前に案内を隠す。読込中は案内を出さず、最後のタブを閉じた時や読込が失敗・中止してタブがない時には案内へ戻す。並行する読込はカウントで扱う。
- PDF/PNG/JPEG/TXT/MDで案内のIsVisibleChangedを監視し、ファイル読込中に一度も表示されないこと、最後のタブを閉じると鶴付き案内へ戻ることを回帰検証。単独起動・外部受信・空の読込要求も確認。全231件（自己42・UI149・起動40）成功。
- Releaseビルドと.NET同梱publish成功。NuGet脆弱性情報取得のNU1900警告のみ。2026-09-26 19:06にProgram Filesへ2.0.16を導入し、artifacts/appとのDLLハッシュ一致を確認。今回は通常Windowsユーザーで起動し、更新前に開いていたPDFを再表示・UI確認した。
- タイミング待ちを追加せず、案内画面だけを省く変更。読込時間そのものがゼロになる変更ではない。GitHubのReleaseは2.0.15のまま。今回の変更は未commit、既存scripts/create-airyview-story.pyの変更は対象外として保持。

## 2.0.17 初期表示の引っかかりを減らす（2026-09-26）

- 利用者の「閉じるときくらい滑らかに開いてほしい」に対応。画像の後続フィット、同じPDFを開いた時の画像破棄、同じPDFページの重複描画を回帰テストで再現し、修正後成功。実際の体感の全原因と断定しない。
- ファイル指定起動は内容（PDFは最初のページの画像）を準備してからウィンドウをShowする。複数指定は最初の1件を表示した後に続ける。パスワード・エラー時は先に親ウィンドウを表示。画像は読込完了時／Loaded時に初回倍率を確定。PDFの同じサイズの進行中Taskを共有し、同じタブの再表示は内容を消さない。
- 全248件（自己42・UI149・起動57）成功。通常／最大化のPDF・PNG・JPG・TXT・MDで内容準備後の表示を確認し、画像は最初のWPF描画フレームのフィットも検証。詳細はdocs/PERFORMANCE-2.0.17.md。
- .NET同梱publish成功。NuGet脆弱性情報取得のNU1900警告のみ。2026-09-26 19:19:53にProgram Filesへ2.0.17を導入。DLLのバージョン2.0.17.0、publishとのSHA256一致（4CE0692F9620641D4B46D9C9BFA1CB4F63EA827315485615F4D5C5B0B0B4C33F）を確認。通常ユーザーでJPEGと4ページの確認用PDFの実表示を確認した。
- 利用者提示のスクリーンショットの元パスは既に存在せず、検証用JPEGと同梱のprint-check.pdfを利用。起動時の案内は維持。体感の滑らかさは実利用で確認が必要。GitHub Releaseは2.0.15のまま、2.0.16〜2.0.17は未commit。既存scripts/create-airyview-story.pyの変更は保護する。

### 2.0.17の公開結果

- 2026-09-26、利用者から「改善しました」と報告があり、GitHub Release更新とpushを明示依頼された。2.0.16〜2.0.17の変更をcommit be10d917ef20aa270e4d4dfd3d9079b9b6bdc05aへまとめてmainにpushし、検証・導入済みの2.0.17を公開した。
- 配布ZIPはAiryView-2.0.17-20260926-192606.zip。SHA256: 43387689B431119E2C182D1E4DE039133B4641C77E2C143BDB9AD2496D618C5D。収録DLLと導入済みDLLの一致を確認。今回と無関係のscripts/create-airyview-story.pyはcommit対象外。
- 公開先: https://github.com/MitsunobuSuhara/AiryView/releases/tag/v2.0.17 。GitHub APIで最新Releaseがv2.0.17、draft=false・prerelease=false、ZIPのuploaded状態・184022658 bytes・SHA256一致を確認。リモートタグv2.0.17はbe10d917ef20aa270e4d4dfd3d9079b9b6bdc05aを指す。
- 配布内容の一覧はartifacts/release-2.0.17-manifest.csv。PDFはSamples/print-check.pdfのみ、PEMは同梱ライブラリのCA証明書。利用者の文書や個人用証明書は含まない。今回の検証・配布物は保持し、削除した一時ファイルはない。

## 2.0.18 ページごとの用紙・向き（2026-09-26）

- 提供された29ページの混在PDFで、A3ページから印刷プレビューを開くと全ページがA3になる症状を再現。従来は閲覧中のページで用紙・向きを1つ選び、全ページへ使っていた。
- 原寸・指定倍率／用紙に合わせる印刷の既定を「ページごとに原本の用紙・向きに合わせる」とした。PrintPaperPlanでページごとのレイアウトとPageSettingsを対にし、実印刷もQueryPageSettingsで切り替える。未対応用紙は対象ページを示して停止。手動指定、N-up、小冊子、ポスターは固定用紙を維持。
- 両面の用紙・向きの変更時は必要な空白裏面を入れ、別サイズのページを同じ紙の表裏へ割り当てない。追加面数と用紙内訳をプレビューへ表示。
- 提供PDFのA4縦17・A4横3・A3横7・A3縦2ページが、プレビューとMicrosoft Print to PDFの実出力で一致。独立したpypdfによる照合でも29ページすべて一致、寸法端数の最大差は約0.085mm。物理プリンターへの送信は行っておらず、給紙・両面の実機確認は利用者に依頼する。
- 全263件（自己42・UI149・起動57・混在印刷15）成功。別途、提供PDFでも混在印刷15件を実行。原本・出力コピー・画面画像はGit外のartifactsだけに保持し、配布へ含めない。仕様と検証の詳細はdocs/PRINTING-2.0.18.md。
- 2026-09-26 19:40:36、管理者確認を経てProgram Filesへ2.0.18を導入。DLLの版2.0.18.0、publishとのSHA256一致（9229A276E59B05DE73F702F069AF171CB5C22C17C39DF3647414D6F89D8616DD）を確認。通常ユーザーで提供PDFの29ページ表示も確認。
- 利用者は「今日のリリースを戻し、今回だけを公開」と依頼。今日のv2.0.15・v2.0.17を下書きへ戻し、v2.0.18を最新の正式リリースとして公開する。以前のリリース・Git履歴・タグは保持する。既存scripts/create-airyview-story.pyは今回もcommit対象外。
